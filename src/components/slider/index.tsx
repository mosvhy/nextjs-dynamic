const Slider = ({ title, description, background, images }: Section) => {
  return (
    <>
      SLIDER
      <h2>{title}</h2>
      <p>{description}</p>
      <p>{background}</p>
      <p>{images}</p>
    </>
  )
};

export default Slider;